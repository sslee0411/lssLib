<UserControl x:Class="lssLib.Serialization.WpfDemo.Views.TypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lssLib.Serialization.WpfDemo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="14">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="입력값" Style="{StaticResource SectionLabel}"/>
                    <TextBox x:Name="Input" Style="{StaticResource InputBox}" Height="32" Margin="0,0,0,10" Text="0x3F800000"/>
                    <Button Content="s.ToInt32()  — 접두사 자동 인식"  Click="OnParseInt"    Style="{StaticResource Btn}"/>
                    <Button Content="s.ToFloat()  — IEEE754 포함"      Click="OnParseFloat"  Style="{StaticResource GreenBtn}"/>
                    <Button Content="s.ToDecimal() — 28~29자리 정밀도" Click="OnParseDecimal" Style="{StaticResource PurpleBtn}"/>
                    <Button Content="s.ToBytes()  — HEX → byte[]"     Click="OnHexToBytes"  Style="{StaticResource OrangeBtn}"/>
                    <Button Content="f.Analyze()  — IEEE754 분석"      Click="OnAnalyze"     Style="{StaticResource TealBtn}"/>
                    <Button Content="s.TryParse / ParseOr"             Click="OnTryParse"    Style="{StaticResource RedBtn}"/>
                    <Button Content="obj.ToJson() / FromJson()"        Click="OnJson"        Style="{StaticResource Btn}"/>
                    <Button Content="list.ToCsv() / FromCsv()"        Click="OnCsv"         Style="{StaticResource GreenBtn}"/>
                    <Button Content="obj.ToXml()  / FromXml()"        Click="OnXml"         Style="{StaticResource PurpleBtn}"/>
                </StackPanel>
            </Border>
        </StackPanel>
        <Border Grid.Column="2" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="Text 결과" Style="{StaticResource SectionLabel}" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Content="지우기" Click="OnClear" Padding="10,3" FontSize="11" Background="#ECEFF1" Foreground="#546E7A" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" Margin="0"/>
                </DockPanel>
                <TextBox Grid.Row="1" x:Name="Out" Style="{StaticResource ResultBox}" MinHeight="500"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

