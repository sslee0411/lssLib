<Window x:Class="lssLib.messaging.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:lssLib.Messaging.Demo.Views"
        Title="lssLib.Messaging Demo"
        Width="1100" Height="760"
        MinWidth="900" MinHeight="640"
        Background="{StaticResource BrushBg}"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="34"/>
        </Grid.RowDefinitions>

        <!-- ── 헤더 ───────────────────────────────────────────── -->
        <Border Grid.Row="0"
                Background="{StaticResource BrushPanel}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚡" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="lssLib.Messaging"
                               Foreground="{StaticResource BrushAccent}"
                               FontSize="18" FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="  —  EventBus · CommandQueue · AsyncScheduler"
                               Foreground="{StaticResource BrushTextDim}"
                               FontSize="12"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 전역 상태 표시 -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <TextBlock Text="큐 대기: "
                               Foreground="{StaticResource BrushTextDim}" FontSize="11"/>
                    <TextBlock x:Name="TxtQueuePending"
                               Text="0"
                               Foreground="{StaticResource BrushWarn}"
                               FontFamily="Consolas" FontSize="11" FontWeight="Bold"
                               Width="28"/>
                    <TextBlock Text="  스케줄: "
                               Foreground="{StaticResource BrushTextDim}" FontSize="11"/>
                    <TextBlock x:Name="TxtSchedulerCount"
                               Text="0"
                               Foreground="{StaticResource BrushAccent2}"
                               FontFamily="Consolas" FontSize="11" FontWeight="Bold"
                               Width="28"/>
                    <TextBlock Text="  구독: "
                               Foreground="{StaticResource BrushTextDim}" FontSize="11"/>
                    <TextBlock x:Name="TxtSubCount"
                               Text="0"
                               Foreground="{StaticResource BrushSuccess}"
                               FontFamily="Consolas" FontSize="11" FontWeight="Bold"
                               Width="28"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── 탭 본문 ─────────────────────────────────────────── -->
        <TabControl Grid.Row="1"
                    Background="{StaticResource BrushBg}"
                    BorderThickness="0"
                    Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Foreground"         Value="{StaticResource BrushTextDim}"/>
                    <Setter Property="Background"         Value="Transparent"/>
                    <Setter Property="BorderThickness"    Value="0"/>
                    <Setter Property="Padding"            Value="18,10"/>
                    <Setter Property="FontSize"           Value="13"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="TabBorder"
                                        Background="Transparent"
                                        BorderThickness="0,0,0,3"
                                        BorderBrush="Transparent"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header"
                                                      HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="BorderBrush"
                                                Value="{StaticResource BrushAccent}"/>
                                        <Setter Property="Foreground"
                                                Value="{StaticResource BrushText}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground"
                                                Value="{StaticResource BrushText}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="📡  EventBus">
                <views:EventBusView x:Name="EventBusView"/>
            </TabItem>

            <TabItem Header="📋  CommandQueue">
                <views:CommandQueueView x:Name="CommandQueueView"/>
            </TabItem>

            <TabItem Header="⏰  AsyncScheduler">
                <views:SchedulerView x:Name="SchedulerView"/>
            </TabItem>
        </TabControl>

        <!-- ── 하단 상태 바 ────────────────────────────────────── -->
        <Border Grid.Row="2"
                Background="{StaticResource BrushPanel}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <TextBlock x:Name="TxtStatusBar"
                           Text="준비"
                           Foreground="{StaticResource BrushTextDim}"
                           FontSize="11"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtClock"
                           Foreground="{StaticResource BrushTextDim}"
                           FontFamily="Consolas" FontSize="11"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
