<!-- ══════════════════════════════════════════════════════════
     IIoT.UI.Themes · Themes/Theme.TerminalGreen.xaml
     테마: ⑥ Terminal Green — CRT 레트로 터미널 / 1980s 제어반
     분위기: 형광 녹색 단색, Share Tech Mono, 스캔라인 질감
     ※ 키 이름은 Theme.DarkNavy.xaml과 100% 동일 유지
     생성: 2025-05-16
     ══════════════════════════════════════════════════════════ -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- §1 ─ 배경 색상 계층 (거의 순수 검정) -->
    <Color x:Key="BgColor">#010A01</Color>
    <Color x:Key="SurfaceColor">#020C02</Color>
    <Color x:Key="CardColor">#030F03</Color>
    <Color x:Key="BorderColor">#082008</Color>
    <Color x:Key="Border2Color">#0D330D</Color>

    <SolidColorBrush x:Key="BgBrush"      Color="{StaticResource BgColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="CardBrush"    Color="{StaticResource CardColor}"/>
    <SolidColorBrush x:Key="BorderBrush"  Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="Border2Brush" Color="{StaticResource Border2Color}"/>

    <!-- §2 ─ 포인트 색상 (형광 녹색 단일) -->
    <Color x:Key="AccColor">#00FF41</Color>   <!-- 터미널 그린 -->
    <Color x:Key="Acc2Color">#00CC33</Color>  <!-- 어두운 그린 -->

    <SolidColorBrush x:Key="AccBrush"      Color="{StaticResource AccColor}"/>
    <SolidColorBrush x:Key="Acc2Brush"     Color="{StaticResource Acc2Color}"/>
    <SolidColorBrush x:Key="AccFaintBrush"  Color="#1500FF41"/>
    <SolidColorBrush x:Key="Acc2FaintBrush" Color="#1500CC33"/>

    <!-- §3 ─ 상태 색상 (모두 녹색 계열) -->
    <Color x:Key="GreenColor">#00FF41</Color>
    <Color x:Key="YellowColor">#FFFF00</Color>  <!-- 터미널 노랑 -->
    <Color x:Key="RedColor">#FF0000</Color>      <!-- 터미널 빨강 -->
    <Color x:Key="OrangeColor">#FF8800</Color>
    <Color x:Key="BlueColor">#00FFFF</Color>     <!-- 시안 -->
    <Color x:Key="PurpleColor">#FF00FF</Color>   <!-- 마젠타 -->

    <SolidColorBrush x:Key="GreenBrush"  Color="{StaticResource GreenColor}"/>
    <SolidColorBrush x:Key="YellowBrush" Color="{StaticResource YellowColor}"/>
    <SolidColorBrush x:Key="RedBrush"    Color="{StaticResource RedColor}"/>
    <SolidColorBrush x:Key="OrangeBrush" Color="{StaticResource OrangeColor}"/>
    <SolidColorBrush x:Key="BlueBrush"   Color="{StaticResource BlueColor}"/>
    <SolidColorBrush x:Key="PurpleBrush" Color="{StaticResource PurpleColor}"/>

    <SolidColorBrush x:Key="GreenFaintBrush"  Color="#1500FF41"/>
    <SolidColorBrush x:Key="YellowFaintBrush" Color="#15FFFF00"/>
    <SolidColorBrush x:Key="RedFaintBrush"    Color="#15FF0000"/>
    <SolidColorBrush x:Key="OrangeFaintBrush" Color="#15FF8800"/>

    <!-- §4 ─ 텍스트 (모두 녹색 계열) -->
    <Color x:Key="TextColor">#00FF41</Color>
    <Color x:Key="Text2Color">#00CC33</Color>
    <Color x:Key="Text3Color">#006616</Color>

    <SolidColorBrush x:Key="TextBrush"  Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="Text2Brush" Color="{StaticResource Text2Color}"/>
    <SolidColorBrush x:Key="Text3Brush" Color="{StaticResource Text3Color}"/>

    <!-- §5 ─ 그라디언트 (단색 계열) -->
    <LinearGradientBrush x:Key="ScaleArrowBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="{StaticResource Acc2Color}" Offset="0"/>
        <GradientStop Color="{StaticResource AccColor}"  Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="PipelineBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="{StaticResource Acc2Color}" Offset="0"/>
        <GradientStop Color="{StaticResource AccColor}"  Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="TopBarBrush" Color="{StaticResource SurfaceColor}"/>

    <!-- §6 ─ 격자 배경 (터미널 그리드) -->
    <DrawingBrush x:Key="GridDotBrush" TileMode="Tile"
                  Viewport="0,0,16,16" ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <GeometryDrawing Brush="#0800FF41">
                <GeometryDrawing.Geometry>
                    <EllipseGeometry Center="1,1" RadiusX="0.6" RadiusY="0.6"/>
                </GeometryDrawing.Geometry>
            </GeometryDrawing>
        </DrawingBrush.Drawing>
    </DrawingBrush>
    <DrawingBrush x:Key="FlowCanvasGridBrush" TileMode="Tile"
                  Viewport="0,0,16,16" ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="#0800FF41">
                    <GeometryDrawing.Geometry>
                        <LineGeometry StartPoint="0,16" EndPoint="16,16"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="#0800FF41">
                    <GeometryDrawing.Geometry>
                        <LineGeometry StartPoint="16,0" EndPoint="16,16"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>

    <!-- §7 ─ 폰트 (레트로 모노 전용) -->
    <!-- Share Tech Mono 미설치 시 Courier New로 대체 -->
    <FontFamily x:Key="MonoFont">Courier New, Consolas</FontFamily>
    <FontFamily x:Key="UiFont">Courier New, Consolas</FontFamily>

    <!-- §8 ─ 치수 (각진 — 0px radius) -->
    <CornerRadius x:Key="Radius">0</CornerRadius>
    <CornerRadius x:Key="RadiusSm">0</CornerRadius>
    <CornerRadius x:Key="RadiusLg">0</CornerRadius>
    <Thickness x:Key="PanelPadding">12,8</Thickness>
    <Thickness x:Key="CardPadding">14</Thickness>
    <Thickness x:Key="FieldMargin">0,0,0,9</Thickness>
    <x:Double x:Key="LeftPanelWidth">220</x:Double>
    <x:Double x:Key="RightPanelWidth">268</x:Double>
    <x:Double x:Key="TopBarHeight">42</x:Double>
    <x:Double x:Key="SubTabHeight">38</x:Double>
    <x:Double x:Key="PanelHeaderHeight">32</x:Double>
    <x:Double x:Key="FontSm">10</x:Double>
    <x:Double x:Key="FontBase">12</x:Double>
    <x:Double x:Key="FontMd">13</x:Double>
    <x:Double x:Key="FontLg">15</x:Double>
    <x:Double x:Key="FontXl">18</x:Double>
    <x:Double x:Key="FontData">22</x:Double>

</ResourceDictionary>
