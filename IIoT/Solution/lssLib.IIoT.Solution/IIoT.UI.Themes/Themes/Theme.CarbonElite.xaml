<!-- ══════════════════════════════════════════════════════════
     IIoT.UI.Themes · Themes/Theme.CarbonElite.xaml
     테마: ⑦ Carbon Elite — 탄소섬유 + 골드 / 프리미엄 럭셔리
     분위기: 샴페인 골드 그라디언트, Cormorant 세리프, 탄소 패턴
     ※ 키 이름은 Theme.DarkNavy.xaml과 100% 동일 유지
     생성: 2025-05-16
     ══════════════════════════════════════════════════════════ -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- §1 ─ 배경 색상 계층 (탄소처럼 순수 어두움) -->
    <Color x:Key="BgColor">#080808</Color>
    <Color x:Key="SurfaceColor">#0F0F0F</Color>
    <Color x:Key="CardColor">#141414</Color>
    <Color x:Key="BorderColor">#1C1C1C</Color>
    <Color x:Key="Border2Color">#2A2A2A</Color>

    <SolidColorBrush x:Key="BgBrush"      Color="{StaticResource BgColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="CardBrush"    Color="{StaticResource CardColor}"/>
    <SolidColorBrush x:Key="BorderBrush"  Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="Border2Brush" Color="{StaticResource Border2Color}"/>

    <!-- §2 ─ 포인트 색상 (샴페인 골드) -->
    <Color x:Key="AccColor">#C9A84C</Color>   <!-- 샴페인 골드 -->
    <Color x:Key="Acc2Color">#E8C87A</Color>  <!-- 밝은 금 -->

    <SolidColorBrush x:Key="AccBrush"      Color="{StaticResource AccColor}"/>
    <SolidColorBrush x:Key="Acc2Brush"     Color="{StaticResource Acc2Color}"/>
    <SolidColorBrush x:Key="AccFaintBrush"  Color="#1AC9A84C"/>
    <SolidColorBrush x:Key="Acc2FaintBrush" Color="#1AE8C87A"/>

    <!-- 골드 그라디언트 브러시 (제목/버튼 전용) -->
    <LinearGradientBrush x:Key="GoldGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource AccColor}"  Offset="0"/>
        <GradientStop Color="{StaticResource Acc2Color}" Offset="1"/>
    </LinearGradientBrush>

    <!-- §3 ─ 상태 색상 -->
    <Color x:Key="GreenColor">#4ADE80</Color>
    <Color x:Key="YellowColor">#FBBF24</Color>
    <Color x:Key="RedColor">#F87171</Color>
    <Color x:Key="OrangeColor">#FB923C</Color>
    <Color x:Key="BlueColor">#60A5FA</Color>
    <Color x:Key="PurpleColor">#A78BFA</Color>

    <SolidColorBrush x:Key="GreenBrush"  Color="{StaticResource GreenColor}"/>
    <SolidColorBrush x:Key="YellowBrush" Color="{StaticResource YellowColor}"/>
    <SolidColorBrush x:Key="RedBrush"    Color="{StaticResource RedColor}"/>
    <SolidColorBrush x:Key="OrangeBrush" Color="{StaticResource OrangeColor}"/>
    <SolidColorBrush x:Key="BlueBrush"   Color="{StaticResource BlueColor}"/>
    <SolidColorBrush x:Key="PurpleBrush" Color="{StaticResource PurpleColor}"/>

    <SolidColorBrush x:Key="GreenFaintBrush"  Color="#1A4ADE80"/>
    <SolidColorBrush x:Key="YellowFaintBrush" Color="#1AFBBF24"/>
    <SolidColorBrush x:Key="RedFaintBrush"    Color="#1AF87171"/>
    <SolidColorBrush x:Key="OrangeFaintBrush" Color="#1AFB923C"/>

    <!-- §4 ─ 텍스트 (따뜻한 오프화이트) -->
    <Color x:Key="TextColor">#F5F0E8</Color>
    <Color x:Key="Text2Color">#C8BFA8</Color>
    <Color x:Key="Text3Color">#5A5448</Color>

    <SolidColorBrush x:Key="TextBrush"  Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="Text2Brush" Color="{StaticResource Text2Color}"/>
    <SolidColorBrush x:Key="Text3Brush" Color="{StaticResource Text3Color}"/>

    <!-- §5 ─ 그라디언트 -->
    <LinearGradientBrush x:Key="ScaleArrowBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="{StaticResource AccColor}"  Offset="0"/>
        <GradientStop Color="{StaticResource Acc2Color}" Offset="0.5"/>
        <GradientStop Color="{StaticResource GreenColor}" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="PipelineBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="{StaticResource AccColor}"   Offset="0"/>
        <GradientStop Color="{StaticResource YellowColor}" Offset="0.6"/>
        <GradientStop Color="{StaticResource GreenColor}" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="TopBarBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#111111" Offset="0"/>
        <GradientStop Color="#080808" Offset="1"/>
    </LinearGradientBrush>

    <!-- §6 ─ 격자 배경 (탄소섬유 대각선 패턴) -->
    <DrawingBrush x:Key="GridDotBrush" TileMode="Tile"
                  Viewport="0,0,6,6" ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <!-- 45도 대각선 탄소 패턴 -->
                <GeometryDrawing Brush="#0C0C0C">
                    <GeometryDrawing.Geometry>
                        <LineGeometry StartPoint="0,6" EndPoint="6,0"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="#0C0C0C">
                    <GeometryDrawing.Geometry>
                        <LineGeometry StartPoint="0,3" EndPoint="3,0"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>
    <DrawingBrush x:Key="FlowCanvasGridBrush" TileMode="Tile"
                  Viewport="0,0,6,6" ViewportUnits="Absolute">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="#0D0D0D">
                    <GeometryDrawing.Geometry>
                        <LineGeometry StartPoint="0,6" EndPoint="6,0"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="#0A0A0A">
                    <GeometryDrawing.Geometry>
                        <LineGeometry StartPoint="3,6" EndPoint="6,3"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>

    <!-- §7 ─ 폰트 (럭셔리 세리프) -->
    <!-- Cormorant Garamond 미설치 시 Georgia로 대체 -->
    <FontFamily x:Key="MonoFont">Georgia, Times New Roman, serif</FontFamily>
    <FontFamily x:Key="UiFont">Georgia, Segoe UI, Malgun Gothic</FontFamily>

    <!-- §8 ─ 치수 (각진 — 0px radius, 명품 느낌) -->
    <CornerRadius x:Key="Radius">0</CornerRadius>
    <CornerRadius x:Key="RadiusSm">0</CornerRadius>
    <CornerRadius x:Key="RadiusLg">0</CornerRadius>
    <Thickness x:Key="PanelPadding">12,8</Thickness>
    <Thickness x:Key="CardPadding">16</Thickness>
    <Thickness x:Key="FieldMargin">0,0,0,9</Thickness>
    <sys:Double x:Key="LeftPanelWidth">220</sys:Double>
    <sys:Double x:Key="RightPanelWidth">268</sys:Double>
    <sys:Double x:Key="TopBarHeight">42</sys:Double>
    <sys:Double x:Key="SubTabHeight">38</sys:Double>
    <sys:Double x:Key="PanelHeaderHeight">32</sys:Double>
    <sys:Double x:Key="FontSm">10</sys:Double>
    <sys:Double x:Key="FontBase">13</sys:Double>   <!-- 세리프는 약간 크게 -->
    <sys:Double x:Key="FontMd">14</sys:Double>
    <sys:Double x:Key="FontLg">16</sys:Double>
    <sys:Double x:Key="FontXl">20</sys:Double>
    <sys:Double x:Key="FontData">24</sys:Double>

</ResourceDictionary>
