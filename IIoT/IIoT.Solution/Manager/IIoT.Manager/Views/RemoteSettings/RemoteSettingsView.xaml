<!-- ══════════════════════════════════════════════════════════
     IIoT.Manager · Views/RemoteSettings/RemoteSettingsView.xaml
     역할: [원격 설정] 탭 — Studio/Collector/Monitor/HMI 의 settings.json 원문을
           NamedPipe 헬스체크 채널(HM-22 확장)로 원격 조회·저장.
     HM-22: 신규
     생성: 2026-07-20
     규칙:
       ① DynamicResource 필수 (버튼: 테마 정식 스타일)
       ② UserControl.Resources 는 UserControl 직계 자식
       ③ Grid 는 Padding 불가 → Border.Padding 사용 (MC3072)
     ══════════════════════════════════════════════════════════ -->
<UserControl x:Class="IIoT.Manager.Views.RemoteSettings.RemoteSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background"      Value="{DynamicResource CardBrush}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius"    Value="10"/>
            <Setter Property="Padding"         Value="16,12"/>
        </Style>
    </UserControl.Resources>

    <Border Padding="24">
        <Grid MaxWidth="760" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- 안내 배너 -->
                <RowDefinition Height="Auto" /> <!-- 대상 선택 + 조회/저장 버튼 -->
                <RowDefinition Height="*" />    <!-- JSON 편집기 -->
                <RowDefinition Height="Auto" /> <!-- 상태 메시지 -->
            </Grid.RowDefinitions>

            <!-- § 안내 배너 -->
            <Border Grid.Row="0" Background="#2A2410" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="#E0A030">
                    <Run Text="⚠ 대상 프로그램이 실행 중이어야 조회·저장이 동작합니다(NamedPipe 헬스체크 채널 재사용)."/>
                    <LineBreak/>
                    <Run Text="저장은 파일만 갱신하며, 대상 프로그램을 재시작해야 변경사항이 적용됩니다."/>
                </TextBlock>
            </Border>

            <!-- § 대상 선택 + 조회/저장 -->
            <Border Grid.Row="1" Style="{StaticResource SectionCard}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="원격 설정 (Remote Settings)" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4" />
                    <TextBlock Text="아래에서 프로그램을 선택하고 [↻ 조회]로 현재 settings.json 내용을 불러온 뒤, 편집기에서 직접 수정하고 [💾 원격 저장]으로 반영하세요."
                               FontSize="11" Foreground="{DynamicResource Text3Brush}"
                               TextWrapping="Wrap" Margin="0,0,0,14" />

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="대상 프로그램"
                                   FontSize="11" Foreground="{DynamicResource Text2Brush}"
                                   VerticalAlignment="Center" />
                        <ComboBox Grid.Column="1" Style="{DynamicResource PropCombo}"
                                  ItemsSource="{Binding Targets}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedItem="{Binding SelectedTarget}" />
                    </Grid>

                    <!-- ★ 규칙: 버튼 활성/비활성은 [RelayCommand(CanExecute=...)] 가
                         자동으로 처리 — 별도 IsEnabled 바인딩/컨버터 불필요 -->
                    <StackPanel Orientation="Horizontal">
                        <Button Content="↻ 조회" Command="{Binding LoadCommand}"
                                Style="{DynamicResource SecondaryBtn}" Margin="0,0,8,0"/>
                        <Button Content="💾 원격 저장" Command="{Binding SaveCommand}"
                                Style="{DynamicResource PrimaryBtn}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- § JSON 편집기 -->
            <Border Grid.Row="2" Style="{StaticResource SectionCard}" Margin="0,0,0,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="settings.json 원문"
                               FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="1" Style="{DynamicResource PropInput}"
                             Text="{Binding JsonText, UpdateSourceTrigger=PropertyChanged}"
                             FontFamily="Consolas" FontSize="12"
                             AcceptsReturn="True" TextWrapping="NoWrap"
                             HorizontalScrollBarVisibility="Auto"
                             VerticalScrollBarVisibility="Auto"
                             VerticalContentAlignment="Top"
                             MinHeight="320"/>
                </Grid>
            </Border>

            <!-- § 상태 메시지 -->
            <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" FontSize="11" TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource GreenBrush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource RedBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

        </Grid>
    </Border>
</UserControl>
